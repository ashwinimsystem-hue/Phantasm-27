/* PHANTASM'27 — GENDER-NEUTRAL PRICING
   Pricing is handled by the application/backend. This legacy display helper is
   intentionally disabled because DOM mutation of registration cards can interfere
   with React state updates and cause the registration page to freeze.
*/
(function () {
  // Intentionally no-op. Gender must never alter the event price.
})();
